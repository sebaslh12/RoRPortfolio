<tr>
    <td><%= blog.title %></td>
    <td>Published <%= distance_of_time_in_words(blog.created_at, Time.now) %> ago</td>
    <td><%= blog.body %></td>
    <td><%= link_to blog.status, toggle_status_blog_path(blog) %></td>
    <td><%= link_to "Show this blog", blog %></td>
    <td><%= link_to "Edit this blog", edit_blog_path(blog) if logged_in?(:site_admin) %></td>
    <td><%= link_to "Destroy this blog", blog_path(blog), data: {
        turbo_method: :delete,
        turbo_confirm: "Are you sure?"} if logged_in?(:site_admin) %></td>
</tr>
